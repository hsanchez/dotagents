# Codex

Codex is approved through `[providers.codex]` in `agents.toml`.

This directory contains Codex-specific assets that dotagents can symlink into
host repos:

- `config.toml` -> `.codex/config.toml`
- `agents/reviewer.toml` -> `.codex/agents/reviewer.toml`
