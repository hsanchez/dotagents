# Claude

Claude is approved through `[providers.claude]` in `agents.toml`.

This directory contains Claude-specific assets that dotagents can symlink into
host repos:

- `settings.json` -> `.claude/settings.json`
