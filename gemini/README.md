# Gemini

Gemini is approved through `[providers.gemini]` in `agents.toml`.

This directory contains Gemini-specific assets that dotagents can symlink into
host repos:

- `settings.json` -> `.gemini/settings.json`
